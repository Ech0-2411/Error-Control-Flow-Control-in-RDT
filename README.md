# Error-Control-Flow-Control-in-RDT
This assignment extends PA1's client-server application to use UDP sockets and implements reliability mechanisms on top of an inherently unreliable transport layer.
